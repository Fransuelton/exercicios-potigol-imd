var numero := leia_inteiro
var total := 0
var soma := 0

para i de 1 até numero faça
	para j de 1 até i faça
    		se i mod j == 0 então
            	total := total + 1
                fim
         fim
         se total == 2 então
         	soma := soma + i
          fim
          total := 0
fim

imprima soma